interface:
  display_name: "Vox Nihili"
  short_description: "Reduce prose to semantic necessity"
  default_prompt: "Use $vox-nihili as the default English-prose doctrine: compose or revise this text at the exact null-voice semantic fixed point."
